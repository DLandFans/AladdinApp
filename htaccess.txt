# BEGIN WordPress
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /

#These are the lines that are needed for the App to process
RewriteRule ^estimates/(\w+)/(\w+)/?$ estimates/app/estimate.php?id=$1&code=$2
RewriteRule ^estimates/(\w+)/?$ estimates/app/estimate.php?id=$1

RewriteRule ^index\.php$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.php [L]
</IfModule>

# END WordPress
